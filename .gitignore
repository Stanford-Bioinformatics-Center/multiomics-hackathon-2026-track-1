# Keep data and figures out of the repository: the pipeline writes them to $HACK_OUT / $HACK_RES / $HACK_FIG
# (outside the repo). Only code and documentation are committed.
*.csv
*.tsv
*.parquet
*.rds
*.png
*.pdf
*.svg
.DS_Store
